# 📐 คู่มือปรับแต่ง Layout PDF

## 🎯 ตำแหน่งและการจัดวาง

### 1️⃣ ปรับตำแหน่ง ขึ้น-ลง (Y Position)

**หลักการ:**
- PDF ใช้พิกัด **Y จากล่างขึ้นบน** (ต่างจาก HTML)
- **เพิ่มค่า Y** = เลื่อนขึ้น | **ลดค่า Y** = เลื่อนลง

**ค่าคงที่สำหรับปรับระยะห่าง:**
```java
// บรรทัด 79-82
private static final float SPACING_AFTER_HEADER = 30f;        // หลัง "บันทึกข้อความ"
private static final float SPACING_BETWEEN_FIELDS = 25f;      // ระหว่างฟิลด์ต่างๆ
private static final float SPACING_BEFORE_SIGNATURES = 40f;   // ก่อนลายเซ็น
private static final float SPACING_BETWEEN_SIGNATURES = 20f;  // ระหว่างลายเซ็น
```

**ตัวอย่างการใช้งาน:**

**ต้องการให้ "บันทึกข้อความ" ห่างจาก logo มากขึ้น:**
```java
// เปลี่ยนบรรทัด 69
private static final float LOGO_SPACING = 80f;  // เดิม 60f
```

**ต้องการให้ฟิลด์ต่างๆ ชิดกันมากขึ้น:**
```java
// เปลี่ยนบรรทัด 80
private static final float SPACING_BETWEEN_FIELDS = 15f;  // เดิม 25f
```

---

### 2️⃣ ปรับตำแหน่ง ซ้าย-ขวา (X Position)

**พิกัด X:**
- `0` = ขอบซ้ายสุด
- `595` = ขอบขวาสุด (ความกว้าง A4)
- `MARGIN_LEFT` (70) = จุดเริ่มต้นมาตรฐาน

**ตัวอย่าง:**

**1. เลื่อน "วันที่" ไปทางซ้าย:**
```java
// บรรทัด 85
private static final float DATE_X_POSITION = PAGE_WIDTH - 250;  // เดิม 200
// ยิ่งเพิ่มตัวเลข = ยิ่งชิดซ้าย
```

**2. เลื่อนลายเซ็นไปทางขวา:**
```java
// บรรทัด 88
private static final float SIGNATURE_X_POSITION = PAGE_WIDTH - MARGIN_RIGHT - 150;  // เดิม 200
// ลดตัวเลข = ชิดขวามากขึ้น
```

**3. ย้าย "บันทึกข้อความ" จากตรงกลางไปขวา:**
```java
// บรรทัด 183-184 แก้เป็น:
float headerTextWidth = fontBold.getStringWidth("บันทึกข้อความ") / 1000 * FONT_SIZE_HEADER;
float headerX = PAGE_WIDTH - MARGIN_RIGHT - headerTextWidth - 50;  // 50 = ระยะห่างจากขอบขวา
yPosition = drawText(contentStream, "บันทึกข้อความ", 
                    fontBold, FONT_SIZE_HEADER, headerX, yPosition);
```

**4. เลื่อนเนื้อหาทั้งหมดไปทางขวา:**
```java
// บรรทัด 62
private static final float MARGIN_LEFT = 100f;  // เดิม 70f
```

---

### 3️⃣ ปรับตัวอักษร หนา-บาง

**ฟอนต์ที่มี:**
- `fontRegular` = ตัวปกติ (THSarabunNew.ttf)
- `fontBold` = ตัวหนา (THSarabunNew Bold.ttf)

**ตัวอย่าง:**

**1. ทำให้ "ส่วนราชการ" เป็นตัวหนา:**
```java
// บรรทัด 195 เปลี่ยนจาก:
yPosition = drawFieldWithUnderline(contentStream, "ส่วนราชการ", govName, 
                             fontRegular, FONT_SIZE_FIELD, 
                             MARGIN_LEFT, yPosition);

// เป็น:
yPosition = drawFieldWithUnderline(contentStream, "ส่วนราชการ", govName, 
                             fontBold, FONT_SIZE_FIELD,  // เปลี่ยนเป็น fontBold
                             MARGIN_LEFT, yPosition);
```

**2. ทำให้ "วันที่" เป็นตัวหนา:**
```java
// บรรทัด 213 เปลี่ยนจาก:
drawText(contentStream, "วันที่ " + date, 
        fontRegular, FONT_SIZE_FIELD, 
        DATE_X_POSITION, fieldStartY);

// เป็น:
drawText(contentStream, "วันที่ " + date, 
        fontBold, FONT_SIZE_FIELD,  // เปลี่ยนเป็น fontBold
        DATE_X_POSITION, fieldStartY);
```

**3. ทำให้เฉพาะป้ายกำกับเป็นตัวหนา แต่ค่ายังเป็นตัวปกติ:**
```java
// สร้าง method ใหม่ (วางหลังบรรทัด 480):
private float drawFieldWithBoldLabel(PDPageContentStream contentStream,
                                    String label,
                                    String value,
                                    PDFont labelFont,
                                    PDFont valueFont,
                                    float fontSize,
                                    float x,
                                    float y) throws IOException {
    // วาดป้ายกำกับด้วยฟอนต์หนา
    contentStream.beginText();
    contentStream.setFont(labelFont, fontSize);
    contentStream.newLineAtOffset(x, y);
    contentStream.showText(label + " ");
    contentStream.endText();
    
    float labelWidth = labelFont.getStringWidth(label + " ") / 1000 * fontSize;
    
    // วาดค่าด้วยฟอนต์ปกติ
    if (value != null && !value.isEmpty()) {
        contentStream.beginText();
        contentStream.setFont(valueFont, fontSize);
        contentStream.newLineAtOffset(x + labelWidth, y);
        contentStream.showText(value);
        contentStream.endText();
    }
    
    return y - fontSize - 5;
}

// ใช้งาน:
yPosition = drawFieldWithBoldLabel(contentStream, "เรียน", recipients, 
                             fontBold, fontRegular, FONT_SIZE_FIELD, 
                             MARGIN_LEFT, yPosition);
```

---

### 4️⃣ ปรับขนาดตัวอักษร

```java
// บรรทัด 75-78
private static final float FONT_SIZE_HEADER = 20f;     // "บันทึกข้อความ"
private static final float FONT_SIZE_FIELD = 14f;      // ฟิลด์ต่างๆ
private static final float FONT_SIZE_CONTENT = 14f;    // เนื้อหา
private static final float FONT_SIZE_SIGNATURE = 14f;  // ลายเซ็น
```

**ตัวอย่าง:**
```java
// ขยาย "บันทึกข้อความ" ใหญ่ขึ้น
private static final float FONT_SIZE_HEADER = 24f;  // เดิม 20f

// ย่อฟิลด์ต่างๆ เล็กลง
private static final float FONT_SIZE_FIELD = 12f;  // เดิม 14f
```

---

### 5️⃣ ปรับขนาดและตำแหน่ง Logo

```java
// บรรทัด 66-69
private static final float LOGO_WIDTH = 120f;   // ความกว้าง
private static final float LOGO_HEIGHT = 40f;   // ความสูง
private static final float LOGO_SPACING = 60f;  // ระยะห่างหลัง logo
```

**ตำแหน่ง Logo (บรรทัด 163-166):**
```java
// ตำแหน่งปัจจุบัน: ซ้ายบน
float logoX = MARGIN_LEFT;

// เปลี่ยนเป็นตรงกลาง:
float logoX = (PAGE_WIDTH - LOGO_WIDTH) / 2;

// เปลี่ยนเป็นขวาบน:
float logoX = PAGE_WIDTH - MARGIN_RIGHT - LOGO_WIDTH;
```

---

### 6️⃣ ปรับขอบกระดาษ

```java
// บรรทัด 60-63
private static final float MARGIN_TOP = 70f;     // ขอบบน
private static final float MARGIN_BOTTOM = 70f;  // ขอบล่าง
private static final float MARGIN_LEFT = 70f;    // ขอบซ้าย
private static final float MARGIN_RIGHT = 70f;   // ขอบขวา
```

**ผลกระทบ:**
- เพิ่ม `MARGIN_LEFT` = เนื้อหาทั้งหมดเลื่อนขวา
- เพิ่ม `MARGIN_TOP` = เนื้อหาทั้งหมดเลื่อนลง

---

## 🎨 ตัวอย่างการปรับแต่งทั่วไป

### ตัวอย่าง 1: ทำให้ฟิลด์ชื่อเป็นตัวหนาทั้งหมด

```java
// บรรทัด 195 - ส่วนราชการ
yPosition = drawFieldWithUnderline(contentStream, "ส่วนราชการ", govName, 
                             fontBold, FONT_SIZE_FIELD,  // เปลี่ยนเป็น fontBold
                             MARGIN_LEFT, yPosition);

// บรรทัด 207 - ที่
yPosition = drawText(contentStream, "ที่ " + referenceNumber, 
                   fontBold, FONT_SIZE_FIELD,  // เป็น fontBold อยู่แล้ว ✓
                   MARGIN_LEFT, yPosition);

// บรรทัด 213 - วันที่
drawText(contentStream, "วันที่ " + date, 
        fontBold, FONT_SIZE_FIELD,  // เปลี่ยนเป็น fontBold
        DATE_X_POSITION, fieldStartY);

// บรรทัด 221 - เรื่อง
yPosition = drawFieldWithUnderline(contentStream, "เรื่อง", title, 
                             fontBold, FONT_SIZE_FIELD,  // เปลี่ยนเป็น fontBold
                             MARGIN_LEFT, yPosition);

// บรรทัด 230 - เรียน
yPosition = drawText(contentStream, "เรียน " + recipients, 
                   fontBold, FONT_SIZE_FIELD,  // เปลี่ยนเป็น fontBold
                   MARGIN_LEFT, yPosition);
```

### ตัวอย่าง 2: ย้าย "บันทึกข้อความ" ไปทางขวา

```java
// แทนที่บรรทัด 182-184 ด้วย:
log.info("Drawing header: บันทึกข้อความ");
float headerTextWidth = fontBold.getStringWidth("บันทึกข้อความ") / 1000 * FONT_SIZE_HEADER;
float headerX = PAGE_WIDTH - MARGIN_RIGHT - headerTextWidth - 50;
yPosition = drawText(contentStream, "บันทึกข้อความ", 
                    fontBold, FONT_SIZE_HEADER, headerX, yPosition);
yPosition -= SPACING_AFTER_HEADER;
```

### ตัวอย่าง 3: เพิ่มระยะห่างระหว่างโลโก้กับหัวข้อ

```java
// บรรทัด 69 เปลี่ยนจาก:
private static final float LOGO_SPACING = 60f;

// เป็น:
private static final float LOGO_SPACING = 100f;  // เพิ่มจาก 60 เป็น 100
```

---

## 📋 เทคนิคการทดสอบ

1. **แก้ไขค่าคงที่ที่บรรทัด 60-88** สำหรับการปรับทั่วไป
2. **กด F5** ที่หน้า test.html เพื่อดูผล
3. **DevTools จะ compile อัตโนมัติ** ใช้เวลา ~2-3 วินาที
4. **ปรับค่าทีละเล็กน้อย** (5-10 pixel) แล้วดูผล

---

## 🔍 ตารางอ้างอิงตำแหน่ง

| ตำแหน่ง | X | Y |
|---------|---|---|
| มุมซ้ายบน | `MARGIN_LEFT` (70) | `PAGE_HEIGHT - MARGIN_TOP` (772) |
| มุมขวาบน | `PAGE_WIDTH - MARGIN_RIGHT` (525) | `PAGE_HEIGHT - MARGIN_TOP` (772) |
| ตรงกลางบน | `PAGE_WIDTH / 2` (297.5) | `PAGE_HEIGHT - MARGIN_TOP` (772) |
| มุมซ้ายล่าง | `MARGIN_LEFT` (70) | `MARGIN_BOTTOM` (70) |
| มุมขวาล่าง | `PAGE_WIDTH - MARGIN_RIGHT` (525) | `MARGIN_BOTTOM` (70) |

---

## 💡 เคล็ดลับสำคัญ

1. **Y Position:**
   - ลด `yPosition` = เลื่อนลง
   - เพิ่ม spacing constants = เพิ่มระยะห่าง

2. **X Position:**
   - `MARGIN_LEFT` = จุดเริ่มต้นซ้าย
   - `PAGE_WIDTH - xxx` = คำนวณจากขวา
   - `PAGE_WIDTH / 2` = ตรงกลาง

3. **Font:**
   - `fontRegular` = ตัวปกติ
   - `fontBold` = ตัวหนา
   - ใช้พารามิเตอร์ที่ 4 ของ `drawText()` และ `drawFieldWithUnderline()`

4. **การทดสอบ:**
   - ปรับค่าที่บรรทัด 60-88 ก่อน (ง่ายที่สุด)
   - ถ้าต้องการละเอียดมาก แก้ที่บรรทัด 147-257
   - ใช้ test.html + F5 สำหรับดูผลทันที

---

## 📞 ตำแหน่งที่สำคัญในโค้ด

- **ค่าคงที่ทั้งหมด:** บรรทัด 60-88
- **โลโก้ ETDA:** บรรทัด 157-175
- **"บันทึกข้อความ":** บรรทัด 182-184
- **ส่วนราชการ:** บรรทัด 193-198
- **ที่:** บรรทัด 207-209
- **วันที่:** บรรทัด 213-216
- **เรื่อง:** บรรทัด 223-226
- **เรียน:** บรรทัด 232-235
- **เนื้อหา:** บรรทัด 241-245
- **ลายเซ็น:** บรรทัด 252-260

---

**หมายเหตุ:** หลังแก้โค้ดแล้ว Spring DevTools จะ auto-reload ภายใน 1-3 วินาที ไม่ต้องรีสตาร์ท!
