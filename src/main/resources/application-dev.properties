# ============================================
# Development Profile Configuration
# ============================================
# ใช้สำหรับ: localhost, development environment
#
# วิธีใช้:
#   ./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
#   หรือ
#   java -jar app.jar --spring.profiles.active=dev

# ปิด API Key authentication สำหรับ development
api.security.enabled=false

# Logging - เปิด DEBUG สำหรับ development
logging.level.th.go.etda.sarabun.pdf=DEBUG

# แสดง SQL queries (ถ้ามี database)
# spring.jpa.show-sql=true
